import React from 'react';
import ReactDOM from 'react-dom/client';
import DiaryApp from './diary-app-v2';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<DiaryApp />);
